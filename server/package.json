{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "npm run build && nodemon --inspect ./server.js",
    "build": "npm install && cd ../client && npm install && npm run build && cd ../server"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cookie-parser": "^1.4.6",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "express-session": "^1.17.3",
    "imap": "^0.8.19"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
